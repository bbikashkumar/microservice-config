spring:
  application:
    name: CONFIG-DEFAULT
eureka:
  instance:
    prefer-ip-address: true
  client:
    # enabled: false
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
